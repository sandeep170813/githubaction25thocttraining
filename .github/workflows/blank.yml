name: myfirstworkflow
on:
 push:
   branches: 
    - production
 workflow_dispatch:    
jobs:
 Build:
  runs-on: ubuntu-latest
  steps:
   - name: myfirstcommand
     run:  sleep 10s
 Deploy1234:
  needs: Build 
  runs-on: ubuntu-latest
  steps:
   - name: myfirstcommand
     run:  echo "first deploy"    